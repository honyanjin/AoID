# 📋 AoID (Association of Independent Developers) 프로젝트 현황 보고서

**작성일자**: 2026년 7월 12일  
**작성대상**: AoID 프로젝트 전체 (Frontend, Backend, Database)  
**문서목적**: 프로젝트의 규모, 기술 스택, 아키텍처 및 도메인별 세부 기능을 조사하여 개발 현황을 체계적으로 파악하고, 최신 변경 사항과 향후 개선 여지를 기록하기 위함.

---

## 📊 1. 프로젝트 규모 및 통계 (Project Metrics)

현재 기준으로 프론트엔드는 **325개 파일 / 80,306줄**, 백엔드는 **137개 파일 / 21,228줄**로 집계됩니다.  
전체 합산은 **462개 파일 / 101,534줄**로, **총 소스 코드 규모 10만 라인(LoC)을 돌파**하였습니다.  
최근 댓글 이미지 첨부 기능 추가 및 삼각성향검사(Triangle Test) 통계 지표 시각화 고도화, 카테고리 관리 기능 등이 추가되면서 소스 코드 라인 수가 업데이트되었습니다.

| 구분 | 파일 개수 (소스 코드 기준) | 총 라인 수 (LoC) | 전일(7/9) 대비 변화 | 주요 기술 요약 |
| :--- | :--- | :--- | :--- | :--- |
| **Frontend** | 325개 (`.tsx`, `.ts`) | **80,306 줄** | 동일 파일수, +1,633줄 증가 | React 19, MUI v7, React Router v7, TanStack Query v5 |
| **Backend** | 137개 (`.ts`) | **21,228 줄** | 동일 파일수, +274줄 증가 | Node.js, Express v5, PostgreSQL (`pg`), QueryBuilder |
| **합계** | **462개** | **101,534 줄** | 동일 파일수, +1,907줄 증가 | TypeScript 기반 풀스택 구조 (10만 LoC 돌파) |
| **DB 마이그레이션** | 46개 (`.js`) | — | +2개 신규 마이그레이션 추가 | node-pg-migrate (삼각검사 카테고리 및 댓글 이미지 컬럼 추가) |

---

## 🛠️ 2. 기술 스택 (Tech Stack)

### 💻 프론트엔드 (Frontend)
- **프레임워크 및 라이브러리**: React 19 (`^19.2.3`), React DOM (`^19.2.3`)
- **라우팅**: React Router v7 (`react-router-dom ^7.9.6`) — `Suspense` + `lazy()` 기반 코드 스플리팅 전면 적용
- **상태 관리 & 데이터 페칭**:
  - 서버 상태: TanStack Query v5 (`@tanstack/react-query ^5.90.11`) — 게시판/관리자 영역에 통합 적용
  - HTTP 클라이언트: Axios (`^1.13.2`)
  - 전역 클라이언트 상태: React Context API (`AuthContext`, `ContainerWidthContext`)
- **UI 및 스타일링**:
  - UI 컴포넌트: Material-UI v7 (`@mui/material ^7.3.5`, `@mui/icons-material ^7.3.5`)
  - 스타일링: Emotion (`@emotion/react ^11.14.0`, `@emotion/styled ^11.14.1`)
  - 애니메이션: Framer Motion (`^12.23.25`)
  - 아이콘: Lucide React (`^0.555.0`)
- **삼각검사 시각화**: HTML5 Canvas 및 Custom React Component (`TriangleCanvas.tsx`)
  - **통계적 시각화**: 리니어 프로그레스 바 위에 전체 그룹의 평균(Mean) 및 표준편차(stdDev) 범위 박스와 편차 경계선 세로 마커 렌더링, 툴팁 표시
- **리치 텍스트 에디터**: React Quill New (`react-quill-new ^3.6.0`), Quill Table Up (`^3.2.2`)
- **결제 연동**: TossPayments SDK (`@tosspayments/payment-widget-sdk ^0.12.1`)
- **차트 및 시각화**: Recharts (`^3.5.1`)
- **날짜 처리**: date-fns (`^4.1.0`)
- **SEO**: react-helmet-async (`^3.0.0`)
- **보안 및 살균**: DOMPurify (`^3.3.0`)
- **빌드 도구**: Create React App (`react-scripts 5.0.1`), TypeScript (`^4.9.5`)

### ⚙️ 백엔드 (Backend)
- **런타임 및 웹 서버**: Node.js, Express v5 (`^5.1.0`)
- **데이터베이스**: PostgreSQL (`pg ^8.16.3`), 마이그레이션 도구 (`node-pg-migrate ^8.0.3`)
- **쿼리 헬퍼**: 자체 `QueryBuilder` 클래스 + `calculatePagination` 유틸 (동적 SQL 생성 및 파라미터 자동 관리)
- **수학적 점수 환산 알고리즘**: 무게중심 좌표(Barycentric Coordinates) 환산 로직 내장 (삼각검사 전용)
- **보안 및 인증**: Passport.js (`^0.7.0`), Google OAuth2 (`passport-google-oauth20`), GitHub OAuth2 (`passport-github2`), JWT (`jsonwebtoken ^9.0.2`), BCrypt (`bcrypt ^6.0.0`), Cookie Parser (`cookie-parser ^1.4.7`)
- **파일 업로드**: Multer (`^2.0.2`)
- **입력 유효성 검사**: express-validator (`^7.3.1`)
- **작업 스케줄링**: node-cron (`^4.2.1`)
- **이메일 전송**: Nodemailer (`^7.0.11`)
- **UUID 생성**: uuid (`^13.0.0`)
- **개발 도구**: ts-node (`^10.9.2`), nodemon (`^3.1.11`), TypeScript (`^5.9.3`)

---

## 📂 3. 디렉토리 구조 및 역할

### 💻 Frontend 디렉토리 (`frontend/src`)

```
frontend/src/
├── App.tsx              (메인 라우터 - 60개+ 라우트 정의)
├── pages/               (27개 페이지 파일, 삼각검사 관련 3개 페이지 탑재, 레거시 2개 삭제됨)
├── components/
│   ├── admin/           (관리자 대시보드 전체 - 20개 서브 도메인)
│   │   └── triangle/    (삼각검사 관리: TriangleTestManagement, TriangleDashboardCustom)
│   ├── common/          (헤더, 푸터, Quill 에디터, ContentTop 등)
│   ├── board/           (게시판 UI)
│   ├── auth/            (인증 가드, OAuth 컴포넌트)
│   ├── home/            (홈 화면 섹션 컴포넌트)
│   ├── my-apps/         (앱 등록 및 타겟 분석)
│   ├── mypage/          (마이페이지 탭 컴포넌트)
│   ├── triangle/        (삼각검사 사용자 전용: TriangleCanvas)
│   ├── devcoins/        (데브코인 관련 UI)
│   └── payment/         (결제 위젯 및 흐름 컴포넌트)
├── contexts/            (AuthContext, ContainerWidthContext)
├── hooks/               (13개 공통 비즈니스 훅)
├── services/            (16개 API 서비스 모듈)
│   └── triangleTestApi.ts (삼각검사 관련 API 연동 - 진행도 임시저장, 결과 일괄저장, 전체 진행현황 일괄조회 포함)
├── queries/             (TanStack Query 훅 및 쿼리 키)
├── types/               (TypeScript 타입 정의)
├── utils/               (공통 유틸리티)
└── constants/           (상수 정의)
```

### ⚙️ Backend 디렉토리 (`backend/src`)

```
backend/src/
├── index.ts             (Express 앱 진입점 - 42개+ API 라우트 등록)
├── config/              (database.ts, env.ts, passport.ts)
├── controllers/         (15개 도메인별 컨트롤러 폴더 및 파일)
│   ├── about/, admin/, app-vision/, auth/
│   ├── board/, business/, cart/, devcoins/
│   ├── email/, home/, member/, payment/
│   ├── settings/, shop/, upload/
│   └── triangleTestController.ts (삼각검사 CRUD, 결과 판정, 임시저장/소거, 이력 검증, 결과지 배치 저장 및 내 모든 진행현황 조회)
├── routes/              (도메인별 라우트 파일들)
│   ├── home/            (10개 홈 위젯 라우트, 모션박스 제거됨)
│   ├── about/           (3개 About 라우트)
│   ├── board/           (postRoutes, commentRoutes)
│   ├── member/          (gradeRoutes, mypageRoutes)
│   ├── settings/        (settingsRoutes, pageContentRoutes, backgroundRoutes)
│   ├── upload/          (uploadRoutes, imageLibraryRoutes, videoLibraryRoutes)
│   ├── business/, app-vision/
│   ├── triangleTestRoutes.ts (삼각검사 API 라우트 - 임시 진행도, 전체 진행현황 목록, 결과지 배치저장 맵핑)
│   └── 핵심 라우트 파일들 (adminRoutes, authRoutes, paymentRoutes, shopRoutes, ...)
├── services/            (17개 비즈니스 로직 서비스, 레거시 1개 삭제됨)
├── middleware/           (auth.ts, caseConverter.ts, uploadMiddleware.ts, validation.ts)
├── scheduler/           (subscriptionScheduler.ts)
├── utils/               (9개 공통 유틸)
│   ├── queryBuilder.ts, pagination.ts, caseConverter.ts
│   ├── crudController.ts, errorHandler.ts, fileManager.ts
│   └── cookie.ts, parseRequest.ts, requestHelpers.ts
└── types/               (TypeScript 타입 정의)
```

### 🗄️ Database 디렉토리 (`backend/database`)
- `migrations/`: **46개** 마이그레이션 파일 (node-pg-migrate 기반)
- `sql_migrations/`: **32개** SQL 기반 마이그레이션 (레거시/보조 스키마)
- `legacy/`: 기존 스키마 파일 보관

---

## 🧩 4. 주요 도메인 및 기능 분석

### 🔐 1) 인증 및 회원 (Auth & Membership)
- **다중 로그인 수단**: 이메일/비밀번호 로그인(BCrypt 해싱), Google OAuth2, GitHub OAuth2 (Passport.js 전략)
- **보안 토큰**: JWT를 **HTTPOnly 쿠키**에 저장하여 XSS 공격 방어. 프론트엔드 Axios 인터셉터로 401 응답 자동 처리
- **이메일 기능**: 회원가입 시 이메일 인증, 비밀번호 찾기 시 임시 비밀번호 발송 (Nodemailer)
- **등급 시스템**: `regular` / `associate` 등 회원 등급별 서비스 이용 범위 차별화 (`gradeService.ts`, `permissionService.ts`)
- **미들웨어**: `authenticate` (JWT 검증), `isAdmin` (관리자 권한) — 모든 보호 라우트에 적용

### 📝 2) Quill 리치 텍스트 에디터 (Rich Text Editor)
- **컴포넌트 위치**: `frontend/src/components/common/rich-text-editor/` (총 13개 서브 디렉토리/파일)
- **핵심 파일**: `RichTextEditor.tsx` (26,704 bytes), `SpecialElementsList.tsx` (71,945 bytes), `EditorWrapper.tsx` (10,864 bytes)
- **주요 기능**:
  - 이미지/비디오 본문 삽입 + 미디어 라이브러리 연동
  - 대용량 비디오 업로드 시 **실시간 진행률(%)** 표시 토스트
  - 표 복사-붙여넣기 시 Quill Table Up 기반 표 구조 보호
  - 중복 미디어 첨부 자동 감지 및 차단
  - `dialogs/`, `popovers/`, `quill-config/`, `components/` 서브 모듈 분리

### 💬 3) 계층형 댓글 및 이미지 첨부 시스템
- **계층형 구조**: 대댓글(자기 참조) 구조, 대댓글이 있을 때 삭제 시 "삭제된 댓글입니다"로 논리 삭제(Soft Delete) 처리
- **[NEW] 댓글 이미지 첨부 기능**:
  - 댓글 작성 및 수정 창에서 카메라 아이콘을 통해 이미지를 선택하면 `imageLibraryAPI.uploadMyImage` API를 거쳐 서버에 업로드 후 `comments` 테이블의 `image_url` 필드에 보존됩니다.
  - 댓글 목록 렌더링 시 이미지 썸네일이 제공되며, 썸네일 우상단 돋보기 아이콘을 통해 원본 크기로 조망할 수 있는 **확대 모달 뷰어(MUI Modal, Backdrop, Fade 연동)**가 동작합니다.
  - 수정 모드에서도 이미지 미리보기, 변경 및 삭제를 동일하게 지원하여 유연한 댓글 미디어 첨부 경험을 제공합니다.

### 🔺 4) 삼각성향검사 (Triangle Test)
전체 모션 박스(Motion Box) 기능을 완벽히 대체하여 신규 추가된 핵심 도메인입니다.
- **수학적 점수 환산 알고리즘**:
  - 삼각형 내부의 한 점 $(x, y)$을 꼭지점 삼각 비중인 무게중심 좌표(Barycentric Coordinates)로 변환합니다. 변환 결과는 A, B, C 세 성향(Traits)의 점수로 환산됩니다.
- **[NEW] 카테고리 관리 및 메뉴 연동**:
  - 삼각성향검사를 카테고리(`triangle_test_categories` 테이블)별로 분류하여 관리할 수 있는 체계가 도입되었습니다.
  - 사용자는 목록 페이지에서 사이드바의 카테고리 메뉴를 접고 펼치며, 슬러그 라우팅(`slug`)을 바탕으로 카테고리별 검사 목록을 편리하게 이동할 수 있습니다.
  - 등급 권한(Grade Permissions) 설정을 강화하여 접근이 금지된 등급의 회원에 대해서는 응시 버튼을 비활성화하는 등 권한 검사를 세밀화했습니다.
- **[NEW] 통계 지표 시각화 및 리니어 프로그레스 바 고도화**:
  - 내부 연산 점수(10,000점 만점)를 화면에는 직관적인 **100점 만점 척도**로 스케일링하여 표시합니다.
  - 리니어 프로그레스 바 위에 해당 성향의 **전체 응시자 평균(Mean)** 및 **표준편차(stdDev) 범위 박스**와 편차 경계 세로선 마커를 정교하게 렌더링합니다.
  - 진행 바 위에 마우스를 올리면 툴팁에 평균값과 표준편차가 시각적으로 표시되어 본인의 성향이 그룹 전체에서 어느 위치에 속하는지 파악할 수 있는 과학적 통계를 지원합니다.
  - 최하점(0점) 또는 최대점(100점)과 같은 극단적 경계면에서 세로선 마커가 잘리거나 툴팁 X좌표 정렬이 어긋나던 UI 렌더링 버그들을 완전 해결했습니다.
- **[NEW] 문항 일괄 편집 및 드롭 인디케이터 재정렬**:
  - 관리자단에서 드래그 앤 드롭을 이용해 문항 순서를 수정할 때 삽입할 공간을 직관적으로 보여주는 **드롭 인디케이터 바(Drop Indicator Bar)**가 동작합니다.
  - `Bulk Questions Dialog`를 활용하여 대량의 질문 목록과 세 꼭짓점 옵션(Vertices Options)을 미니 테이블 형태로 한눈에 편집해 일괄 등록할 수 있습니다.
  - 백엔드에서는 데이터 일관성을 지키기 위해 여러 개의 질문을 트랜잭션으로 묶어 배치 저장하는 `saveBulkQuestions` API를 구현 완료했습니다.
- **실시간 임시 진행도 저장 및 로드**:
  - 사용자가 문제를 풀다가 이탈할 경우를 대비하여 `triangle_test_progress` 테이블을 통한 자동 세션 보존이 구현되었습니다. (1.5초 디바운스 자동 저장)
- **제출 완료 시 재진입 차단**:
  - 성향검사 최종 제출(`submitTest`) 완료 시 임시 저장 데이터를 영구 파괴하고, 질문지 진입 시 최신 `lastSubmissionId`를 반환해 강제 결과지 리다이렉트(`replace: true`) 및 뒤로가기 재진입을 원천 차단합니다.
- **결과 리포트 페이지 2행 미니멀 레이아웃 및 말풍선 프로그레스 바**:
  - 가독성을 극대화하기 위해 분석 및 결과 유형 텍스트 위주의 **2행 미니멀 레이아웃**을 적용하였으며, 황금색/인디고색 툴팁 말풍선 칩이 달린 인라인 프로그레스 바로 수치 가시성을 극대화했습니다.
  - `getSubmissionResult` API에서 작성자 프로필 정보(Username, Avatar URL) 및 성향 설명(`description`)을 맵핑하여 해설지의 정보 완성도를 보강했습니다.

### 📑 5) 게시판 및 커뮤니티 (Board & Community)
- **다중 게시판 유형**: 공지사항, Q&A, 자유게시판, 기술공유, 프로젝트협업 등 9개+ 기본 카테고리
- **관리자 카테고리 동적 관리**: `CategoryManagement.tsx`, `MenuGroupManagement.tsx`로 메뉴 그룹 및 카테고리 실시간 추가/수정/삭제
- **게시글 태그 / 임시저장**: `tags`, `representativeTag` 필드 및 `is_temp` 임시저장 필드 지원
- **게시판 전파 규칙**: `board_propagation_rules` 테이블로 카테고리 간 게시글 전파 설정
- **TanStack Query 통합**: `usePosts`, `useAdminPosts`, `useCategories` 훅으로 서버 상태 캐싱 및 무효화

### 🏪 6) 데브숍 & TossPayments 결제 (DevShop & Payment)
- **상품 및 유형 관리**: `ProductManagement.tsx`, `ProductTypeManagement.tsx`, `PurchaseManagement.tsx` 지원
- **장바구니**: `CartPage.tsx` + `cartApi.ts` + `cartRoutes.ts` 연동
- **결제 흐름**: TossPayments 위젯 → 결제 요청 → `/api/payments/confirm` 콜백 → 결과 페이지 (success/fail)
- **구독 모델**: 정기 멤버십 구독 (`subscriptionScheduler.ts`로 만료 주기 처리)
- **데브코인**: 자체 가상 재화 시스템 — 충전, 사용 이력, TossPayments+데브코인 복합 결제 지원 (`devcoinsService.ts`)

### 🌐 7) 홈 화면 위젯 시스템 (Home Widgets)
총 **10개** 독립 위젯으로 구성된 동적 홈 화면 구성 시스템 (기존 모션박스 위젯 삭제): 히어로, 퀵박스, 공지박스, 프로그램박스, 회원앱박스, 프로모카드, 최근게시글박스, 최근댓글박스, CTA섹션, 토글박스.

### 👤 8) 마이페이지 (MyPage)
탭 기반 단일 페이지로 구성, 8개 서브 컴포넌트: `MyProfile`, `MySettings`, `MySubscription`, `MyDevCoins`, `MyPosts`, `MyComments`, `MyLikes`, `MyItems`.

---

## 🔧 5. 백엔드 아키텍처 세부

### API 라우트 전체 목록 (`backend/src/index.ts` 기준)
총 **43개+** API 엔드포인트 그룹:

```
/api/auth               — 인증 (로그인, 회원가입, OAuth)
/api/posts              — 게시글 CRUD
/api/comments           — 댓글 CRUD (이미지 첨부 지원)
/api/admin              — 관리자 기능
/api/admin/grades       — 회원 등급 관리
/api/hero               — 히어로 위젯
/api/settings           — 전역 설정
/api/header             — 헤더 설정
/api/quickbox           — 퀵박스 위젯
/api/noticebox          — 공지박스 위젯
/api/programbox         — 프로그램박스 위젯
/api/memberappbox       — 회원앱박스 위젯
/api/upload             — 파일 업로드
/api/images             — 이미지 라이브러리
/api/videos             — 비디오 라이브러리
/api/organization       — 조직도 (About)
/api/articles           — 언론기사 (About)
/api/ci                 — CI/BI (About)
/api/backgrounds        — 배경 이미지 설정
/api/business-items     — 주요사업 아이템
/api/mypage             — 마이페이지 데이터
/api/appvisions         — 앱비전 (CRUD)
/api/promocard          — 프로모카드 위젯
/api/recentpostbox      — 최근게시글박스 위젯
/api/recentcommentbox   — 최근댓글박스 위젯
/api/ctasection         — CTA섹션 위젯
/api/payments           — 결제 (TossPayments)
/api/shop               — 데브숍 상품
/api/devcoins           — 데브코인
/api/cart               — 장바구니
/api/triangle-tests     — 삼각성향검사 (카테고리, 임시저장, 일괄등록, 결과조회, 통계데이터 포함)
/api/sitemap.xml        — 동적 SEO 사이트맵
/api/pageContent        — 페이지 콘텐츠 설정
```

### 유틸리티 핵심 모듈
- **`queryBuilder.ts`**: 동적 SQL 필터 조건 선언적 구성, `$N` 인덱스 자동 관리
- **`pagination.ts`**: 페이지/카운트 쿼리 결합 처리
- **`caseConverter.ts`**: snake_case ↔ camelCase 자동 변환 (Express 전역 미들웨어 및 헬퍼)
- **`crudController.ts`**: 공통 CRUD 패턴 추상화
- **`errorHandler.ts`**: Express 에러 처리 미들웨어

---

## 🗄️ 6. 데이터베이스 마이그레이션 현황

총 **46개** 마이그레이션 파일 (node-pg-migrate 기반, `backend/database/migrations/`):

| SN | 마이그레이션 파일명 | 내용 |
|----|---|---|
| 1-33 | `initial-baseline` ~ `drop-motion-box-tables` | 초기 코어 스키마, 회원 등급, 메뉴, 위젯 및 모션박스 테이블 완전 삭제 |
| 34 | `create-triangle-tests` | 삼각성향검사 및 문제/답변/제출 기본 테이블 생성 |
| 35 | `add-slug-to-triangle-tests` | 삼각성향검사 Slug 필드 추가 |
| 36 | `create-triangle-test-results` | 삼각성향검사 결과지 정의 테이블 생성 |
| 37 | `create-triangle-test-dynamic-traits` | 동적 성향(Traits) 테이블 및 결과지 조건 매핑 테이블 생성 |
| 38 | `add-allowed-grades-and-limitations-to-triangle-tests` | 삼각검사별 허용 등급 및 응시 제한 컬럼 추가 |
| 39 | `create-triangle-test-dashboard-layout` | 삼각검사 관리자용 대시보드 동적 레이아웃 저장 테이블 생성 |
| 40 | `add-style-and-cover-to-triangle-tests` | 삼각성향검사 시트 대표 이미지 및 외형 설정 테마 컬럼 추가 |
| 41 | `create-triangle-test-progress` | 삼각성향검사 중도 진행 상황 임시 저장용 테이블 생성 |
| 42 | `change_triangle_test_results_to_trait_ranges` | 결과지 구간 조건 매핑 개편 및 종합 성향 점수 필드 제거 |
| 43 | `add-cover-image-position-to-triangle-tests` | 삼각성향검사 커버 이미지 세로 정렬 기준 위치 설정 필드 추가 |
| 44 | `add-cover-image-object-position-to-triangle-tests` | 삼각성향검사 커버 이미지 가로/오브젝트 정렬 기준 위치 설정 필드 추가 |
| **45** | `create-triangle-test-categories` | **삼각성향검사 카테고리(`triangle_test_categories`) 테이블 생성 및 `category_id` 추가** |
| **46** | `add-image-url-to-comments` | **`comments` 테이블에 이미지 첨부용 `image_url` 필드 추가** |

---

## ⚠️ 7. 기술 부채 및 개선 권장 항목

### 🔴 해결 필요 항목
- *(해결 필요 레거시 파일 삭제 항목이 모두 처리되어 현재 잔존하는 🔴 해결 필요 항목이 없습니다.)*

### 🟡 개선 권장 항목
1. **비대화된 단일 파일 분리**:
   - `MyAppForm.tsx` (96,132 bytes) — 앱 등록 폼의 각 섹션 분리 권장
   - `PostFormPage.tsx` (50,046 bytes) — 에디터 관련 로직 분리 여지
   - `TriangleTestManagement.tsx` (46,734 bytes) — 문항 관리/결과지 관리 탭 단위 파일 분리 권장
2. **테스트 코드 부재**: 백엔드의 `test` 스크립트가 placeholder 상태 — 자동화 테스트(Jest, Supertest) 도입 필요
3. **빌드 도구 현대화**: Create React App은 장기적으로 지원 중단 예정 — Vite 마이그레이션 검토 권장

---

## 🚀 8. 배포 및 인프라

- **배포 방식**: GitHub Actions를 통한 자동 CI/CD 파이프라인 (git push → 자동 배포)
- **프로덕션 환경**: AWS 서버 (SSH 접근 가능)
- **DB 변경 반영 순서**: 로컬 DB 먼저 → 사용자 확인 → AWS 프로덕션 DB 반영
- **프론트엔드 포트**: 3000 (개발), React CRA 빌드 (`npm start`)
- **백엔드 포트**: 5000 (Express), `npm run dev` (nodemon + ts-node)
- **Graceful Shutdown**: SIGTERM/SIGINT 신호 수신 시 스케줄러 정리 → HTTP 서버 종료 → DB 풀 종료 순서로 안전 종료
- **광고 수익화**: Google AdSense 연동 — Publisher ID: `pub-8370196278440513`, 사이드바 광고 단위: `3858416151`
- **SEO**: `react-helmet-async` 기반 동적 메타태그 + `/api/sitemap.xml` 동적 사이트맵 제공
