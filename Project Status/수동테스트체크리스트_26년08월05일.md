# 수동 테스트 체크리스트 — 리팩토링 회귀 검증 (2026-08-05)

> **대상**: 7개 관리 컴포넌트 분할 + 백엔드 DevSpectrum 팩토리 통합 + DevSpectrum 프론트 미니훅
> **근거**: `프로젝트현황_26년08월05일_1번.md` 5.2(리팩토링 회귀 검증) / 6(검증 항목)
> **검증 수준**: `tsc --noEmit` + CI 빌드(ESLint)는 통과했지만 **실제 클릭 흐름은 수동 확인 필요**
> **우선순위**: ⭐ = 결제/데이터 저장이 포함된 고위험 경로 (반드시 검증)

---

## 0. 사전 준비 (공통)

- [ ] **환경**: 로컬 백엔드(`backend npm run dev`) + 프론트(`frontend npm start`) 실행
- [ ] **계정**: 관리자 계정(admin role)으로 로그인 → 관리자 대시보드 진입
- [ ] **일반 사용자 계정**도 준비 (ShopPage 결제/마이페이지 검증용)
- [ ] **데이터 준비**: 게시글·상품·홍보카드·로고 이미지·검사 시트가 각 2건 이상 존재
- [ ] **브라우저**: Chrome 기준. 뷰포트별 검증은 DevTools 반응형 모드 사용
- [ ] 각 항목 완료 시 체크박스에 `[x]`, 실패 시 실패 화면 캡처 + 재현 경로 메모

---

## A. 관리자 컴포넌트 분할 회귀 검증 (7종)

### A-1. 게시판 관리 — PostsManagement (493줄 → 훅 527 + 다이얼로그)

**진입**: 관리자 대시보드 → 게시판 관리 → 게시글 관리

| # | 테스트 항목 | 절차 | 기대 결과 | 체크 |
|---|---|---|---|---|
| A1-1 | 목록 로딩 | 페이지 진입 | 로딩 스피너 → 게시글 목록 정상 표시 (useEffect→fetchData) | □ |
| A1-2 | 검색 | 검색어 입력 후 Enter | 목록이 검색어 기준으로 갱신 (`handleSearchKeyPress`) | □ |
| A1-3 | 페이지네이션 | 페이지 번호/rows-per-page 변경 | 목록·카운트 갱신 (`handlePageChange`/`handleRowsPerPageChange`) | □ |
| A1-4 | ⭐ 상태 변경 | 게시글의 공개/임시 토글 클릭 | 즉시 토글 + toast, 새로고침 후에도 유지 (`handleInlinePublishToggle`/`handleInlineTempToggle`) | □ |
| A1-5 | ⭐ 삭제 | 삭제 버튼 → 확인 다이얼로그 → 확정 | 다이얼로그 열림 → 삭제 완료 toast → 목록에서 제거 (`handleOpenDeleteDialog`/`handleDelete`) | □ |
| A1-6 | 일괄 작업 | 체크박스 다중 선택 → 카테고리 변경/작성자 변경/삭제/공개상태 변경 | 선택된 게시글에만 적용 (`handleSelectAll`/`handleBulk*`) | □ |
| A1-7 | 마크다운 임시등록 | 마크다운 붙여넣기 → 파싱 → 등록 다이얼로그 | 파싱 결과 미리보기 정상 (`handleParseMarkdown`/`handleBulkImportSubmit`) | □ |
| A1-8 | 메모 수정 | 메모 아이콘 클릭 → 수정 | 저장 후 표시 갱신 (`handleEditMemo`) | □ |
| A1-9 | 작성자 필터 | 작성자 선택/사전선택 | 필터 적용 (`handleAuthorChange`/`handlePreselectedAuthorChange`) | □ |

### A-2. 구매 관리 — PurchaseManagement (1,177줄 → 훅 365 + 다이얼로그)

**진입**: 관리자 대시보드 → 샵 관리 → 구매 관리

| # | 테스트 항목 | 절차 | 기대 결과 | 체크 |
|---|---|---|---|---|
| A2-1 | 주문 목록/상세 | 주문 행 클릭 → 상세 다이얼로그 | 상세 정보 정상 (`handleViewPurchaseDetail`) | □ |
| A2-2 | ⭐ 환불 (전액) | 환불 버튼 → 전액 선택 → 확인 | 환불 완료 toast + 상태 갱신 (`handleRefund`) | □ |
| A2-3 | ⭐ 환불 (부분) | 환불 버튼 → 부분 금액 입력 → 확인 | 부분 금액만 환불 처리 | □ |
| A2-4 | ⭐ 주문 취소 | 취소 다이얼로그 → 확정 | 취소 처리 (`handleOpenCancelOrderDialog`/`handleCancelOrder`) | □ |
| A2-5 | 주문 삭제 | 삭제 다이얼로그 → 확정 | 삭제 완료 (`handleOpenDeleteDialog`/`handleDeleteOrder`) | □ |
| A2-6 | 구독 상세 | 구독 행 클릭 → 상세 | 다음 결제일 등 정상 (`handleViewSubscriptionDetail`) | □ |
| A2-7 | ⭐ 구독 다음 결제 중지 | 중지 다이얼로그 → 확정 | 중지 처리 toast (`handleOpenCancelSubscriptionDialog`/`handleCancelSubscription`) | □ |
| A2-8 | ⭐ 구독 재활성화 | 재활성화 버튼 → 확인 | 재활성화 처리 (`handleReactivateSubscription`) | □ |
| A2-9 | 비활성화 | 비활성화 다이얼로그 → 확정 | 상태 갱신 (`handleOpenDeactivateDialog`/`handleDeactivate`) | □ |

### A-3. 상품 관리 — ProductManagement (1,098줄 → 훅 421 + 다이얼로그)

**진입**: 관리자 대시보드 → 샵 관리 → 상품 관리

| # | 테스트 항목 | 절차 | 기대 결과 | 체크 |
|---|---|---|---|---|
| A3-1 | 상품 목록/통계 | 탭 진입 | 통계 카드 + 상품 테이블 정상 | □ |
| A3-2 | ⭐ 상품 추가 | 추가 버튼 → 폼 입력 → 저장 | 저장 성공 toast + 목록 갱신 (`handleOpenDialog`+`handleSubmit`) | □ |
| A3-3 | ⭐ 상품 수정 | 수정 버튼 → 값 변경 → 저장 | 변경값 반영 (`handleOpenDialog`의 metadata JSON.parse) | □ |
| A3-4 | 상품 활성/비활성 | 토글 | 즉시 갱신 + 새로고침 후 유지 (`handleToggleActive`) | □ |
| A3-5 | ⭐ 삭제 | 삭제 → 확인 다이얼로그 | 삭제 완료 (`handleDeleteClick`/`handleDeleteConfirm`) | □ |
| A3-6 | 이미지 라이브러리 | 이미지 선택 버튼 → 라이브러리 → 선택 | 이미지 적용 (`handleOpenImageLibrary`/`handleSelectImage`) | □ |
| A3-7 | 이미지 업로드/제거 | 파일 업로드, 제거 버튼 | 미리보기 갱신 (`handleImageUpload`/`handleClearImage`) | □ |
| A3-8 | 유형 선택 | 유형 탭/선택 다이얼로그 | 유형별 상품 필터 (`handleTypeSelected`) — **구독 시 기본 기간 값 확인** | □ |
| A3-9 | 특징 항목 | 특징 추가/제거 버튼 | 목록 증감 (`handleAddFeature`/`handleRemoveFeature`) | □ |
| A3-10 | devcredits/구독 분기 | devcredits 상품·구독 상품 각각 저장 | 각 타입별 필드(가격/기간) 정상 저장 | □ |

### A-4. 헤더 관리 — HeaderManagement (1,228줄 → 훅 457 + 패널 4 + 다이얼로그)

**진입**: 관리자 대시보드 → 홈 관리 → 헤더 관리

| # | 테스트 항목 | 절차 | 기대 결과 | 체크 |
|---|---|---|---|---|
| A4-1 | 대메뉴 표시 토글 | 각 대메뉴 토글 | 토글 즉시 반영 + 저장 후 유지 (`handleMenuVisibilityChange`/`handleSaveMenuVisibility`) | □ |
| A4-2 | ⭐ 대메뉴 순서 드래그앤드롭 | 메뉴 행 드래그 → 순서 변경 → 저장 | 드래그 중 하이라이트, 놓으면 순서 변경, 저장 시 유지 (`handleDragStart`/`handleDragOver`/`handleDrop`/`handleSaveMenuOrder`) | □ |
| A4-3 | ⭐ 전역 설정 | 표시/고정/높이/배경색 변경 → 저장 | 헤더 미리보기 실시간 반영 + 저장 유지 (`handleGlobalSettingsChange`/`handleSave`) | □ |
| A4-4 | ⭐ 로고 업로드 | 파일 선택 → 미리보기 → 저장 | 업로드 성공 + 실제 헤더에 반영 (`handleFileChange`/`handleSave`) | □ |
| A4-5 | 로고 라이브러리 | 로고 선택 버튼 → 라이브러리 → 선택 | 로고 적용 (`handleOpenLogoPicker`/`handleConfirmLogoSelection`) | □ |
| A4-6 | 기본 로고 사용 | 기본 로고 버튼 | 기본 로고 복원 (`handleUseDefaultLogo`) | □ |
| A4-7 | 제목/부제목 | 값 변경 → 저장 | 헤더에 반영 (`handleFieldChange`) | □ |

### A-5. 홍보카드 관리 — PromoCardManagement (1,354줄 → 훅 402 + 패널 2 + 다이얼로그 2)

**진입**: 관리자 대시보드 → 홈 관리 → 홍보카드 관리

| # | 테스트 항목 | 절차 | 기대 결과 | 체크 |
|---|---|---|---|---|
| A5-1 | 카드 목록/순서 | 탭 진입 → 순서 변경 → 저장 | 순서 변경 반영 (`handleReorder`) | □ |
| A5-2 | ⭐ 카드 추가 | 추가 → 기본/스타일 탭 입력 → 저장 | 카드 생성 + 홈에 표시 (`handleOpenDialog`+`handleSaveCard`) | □ |
| A5-3 | ⭐ 카드 수정 | 수정 → 내용 변경 → 저장 | 변경 반영 (`handleOpenDialog`의 메타데이터 매핑) | □ |
| A5-4 | ⭐ 카드 활성 토글 | 토글 클릭 | 즉시 반영 + 유지 (`handleToggleActive`) | □ |
| A5-5 | 카드 삭제 | 삭제 → 확인 | 삭제 완료 (`handleDeleteCard`) | □ |
| A5-6 | 배경 이미지 3분기 | (1) 업로드 (2) 라이브러리 (3) URL/제거 각각 시도 | 3가지 방식 모두 미리보기·저장 정상 (`handleBgImageFileChange`/`handleSelectLibraryImage`/`handleRemoveBgImage`) | □ |
| A5-7 | 스타일 설정 | 섹션 제목/카드 스타일 변경 → 저장 | 실시간 미리보기 + 저장 유지 (`handleSettingsChange`/`handleSaveSettings`) | □ |
| A5-8 | 아이콘 선택 | 아이콘 옵션 변경 | 카드 아이콘 반영 (`getIconComponent`) | □ |

### A-6. ⭐ 샵 페이지 — ShopPage (1,188줄 → 훅 522 + 카드 + 결제 다이얼로그) — 사용자 페이지

**진입**: 사이트 메인 → 샵 (일반 사용자 계정으로도 검증)

| # | 테스트 항목 | 절차 | 기대 결과 | 체크 |
|---|---|---|---|---|
| A6-1 | 상품 카드 상태 표시 | 보유/구독중/품절/N회 한도 상품 각각 확인 | 상태 배지·버튼 표시 정상 (`ShopProductCard` 계산 로직) | □ |
| A6-2 | 장바구니 담기 | 담기 버튼 → 카트 아이콘 | 수량 배지 갱신 (`handleAddToCart`) | □ |
| A6-3 | ⭐ DevCredits 결제 | DevCredits 상품 → 구매 → DevCredits 결제 | 결제 진행 모달 → 성공 다이얼로그 (`handleDevCreditsPurchase`) | □ |
| A6-4 | ⭐ 카드 결제 | 카드 상품 → 구매 → 카드 결제(실 결제) | Toss 위젯 모달 → 성공/실패 다이얼로그 (`handleRealPayment`/`handlePaymentSuccess`/`handlePaymentError`) | □ |
| A6-5 | ⭐ 원클릭 결제 | 카드만 허용 상품에서 즉시 결제 | 다이얼로그 없이 바로 결제 진행 | □ |
| A6-6 | ⭐ 결제 실패 | 실패 유발(카드 정보 오류) | 실패 다이얼로그 + 재시도 버튼 동작 | □ |
| A6-7 | ⭐ 새로고침 복원 | 결제 모달 중 새로고침 | sessionStorage 기반 결제 상태 복원 (useEffect) | □ |
| A6-8 | 구매 후 상태 | 결제 완료 후 상품 카드 | "보유" 상태로 전환 | □ |
| A6-9 | 탭 전환 | 전체/카테고리 탭 | 목록 갱신 (`handleTabChange`) | □ |

### A-7. 마이페이지 설정 — MypageGlobalSettings (1,056줄 → 훅 281 + 탭 4 + 다이얼로그)

**진입**: 관리자 대시보드 → 회원 관리 → 마이페이지 설정

| # | 테스트 항목 | 절차 | 기대 결과 | 체크 |
|---|---|---|---|---|
| A7-1 | 배경 설정 탭 | 배경 이미지/색상/높이/오버레이 변경 | 미리보기 반영 (`handleImageUpload`/`handleChange`) | □ |
| A7-2 | 제목 설정 탭 | 제목 스타일/애니메이션 변경 | 미리보기 반영 | □ |
| A7-3 | 빵메뉴 설정 탭 | 빵메뉴 표시/스타일 변경 | 미리보기 반영 | □ |
| A7-4 | 메뉴 노출/정렬 | 메뉴 토글/이동 버튼 | 순서·노출 변경 (`handleMenuToggle`/`handleMoveMenu`) | □ |
| A7-5 | ⭐ 커스텀 메뉴 추가 | 추가 → 이름/타입 입력 → 저장 | 목록에 추가 (`handleOpenAddDialog`+`handleSaveNewMenu` — **logout 앞 삽입 로직 확인**) | □ |
| A7-6 | ⭐ 커스텀 메뉴 수정 | 수정 → 변경 → 저장 | 변경 반영 (`handleOpenEditDialog`) | □ |
| A7-7 | 커스텀 메뉴 삭제 | 삭제 버튼 | 목록에서 제거 (`handleDeleteMenu`) | □ |
| A7-8 | ⭐ 전체 저장 + 새로고침 | 저장 버튼 → 새로고침 → 재진입 | 모든 탭 설정 유지 (`handleSave`) | □ |
| A7-9 | 기본값 초기화 | 기본값 버튼 | 설정이 기본값으로 복원 | □ |

---

## B. 백엔드 DevSpectrum 팩토리 통합 검증

**대상**: `backend/src/utils/testCrudUtils.ts`(팩토리 13종) + 프론트 미니훅 3종(`useDevSpectrumTraitCrud`/`useDevSpectrumSubmissionOps`/`useDevSpectrumResultTransfer`)
**공통 절차**: 관리자 대시보드 → 데브스펙트럼 → 각 검사(Triangle / MultipleChoice / BarScale) 관리 화면
**핵심 확인점**: **3개 검사가 모두 동일하게 동작**하는지 (공통 팩토리 회귀가 한 곳에만 영향 주는 경우 방지)

### B-1. 검사 CRUD 공통 (createCommonTest/updateCommonTest/deleteCommonTest/toggleCommonTestActive)

| # | 테스트 항목 | 절차 (각 검사마다) | 기대 결과 | TRI | MC | BAR |
|---|---|---|---|---|---|---|
| B1-1 | 검사 생성 | 새 검사 작성 → 저장 | 시트 목록에 추가 | □ | □ | □ |
| B1-2 | ⭐ 검사 수정 | 태그/질문 수정 → 저장 | 태그 배열(`formatTags`) 정상 저장, 빈 태그 처리 | □ | □ | □ |
| B1-3 | 검사 활성/비활성 토글 | 토글 | 상태 유지 | □ | □ | □ |
| B1-4 | 검사 삭제 | 삭제 → 확인 | 목록에서 제거 | □ | □ | □ |
| B1-5 | ⭐ 검사 응시 자격 | 미응시/응시완료 사용자 각각 제출 시도 | 자격 로직 정상 (`checkTestEligibility`) | □ | □ | □ |

### B-2. 성향 CRUD (createTraitHandlers) + 미니훅 useDevSpectrumTraitCrud

| # | 테스트 항목 | 기대 결과 | TRI | MC | BAR |
|---|---|---|---|---|---|
| B2-1 | 성향 추가 | 목록에 추가 | □ | □ | □ |
| B2-2 | 성향 수정 | 이름/설명 변경 저장 | □ | □ | □ |
| B2-3 | 성향 삭제 | 삭제 + 연관 데이터 영향 확인 | □ | □ | □ |
| B2-4 | 성향 활성 토글 | 토글 반영 | □ | □ | □ |
| B2-5 | ⭐ 쌍(트레이트 페어) CRUD | 페어 추가/수정/삭제 (`createTraitPairHandlers`) | □ | □ | □ |
| B2-6 | ⭐ 진행률 설정 | 질문별 진행률 표시 값 (`createProgressHandlers`) | □ | □ | □ |

### B-3. 제출물 관리 (createSubmissionHandlers) + 미니훅 useDevSpectrumSubmissionOps

| # | 테스트 항목 | 기대 결과 | TRI | MC | BAR |
|---|---|---|---|---|---|
| B3-1 | ⭐ 제출물 공개/비공개 전환 (`updateSubmissionPublic`) | 공개 시 타인 조회 가능, 비공개 시 불가 (**BAR: 비공개 접근 제어가 TRI와 동일하게 동작하는지**) | □ | □ | □ |
| B3-2 | 제출물 삭제 | 삭제 완료 | □ | □ | □ |
| B3-3 | 관리자 제출물 목록 (`getAdminSubmissions`) | 전체 제출물 목록 정상 | □ | □ | □ |
| B3-4 | 내 제출물 목록 (`getMySubmissions`) | 본인 제출물만 표시 | □ | □ | □ |
| B3-5 | ⭐ 소유권 검증 | **타인 계정으로 남의 제출물 수정/삭제 시도** | 403/거부 (팩토리 통일된 검증) | □ | □ | □ |
| B3-6 | ⭐ 관리자 검증 | 일반 계정으로 관리자 API 호출 | 403/거부 | □ | □ | □ |

### B-4. ⭐ 종합분석 (createOverallAnalysisHandlers + createOverallAnalysisMatcher) + useDevSpectrumResultTransfer

| # | 테스트 항목 | 기대 결과 | TRI | MC | BAR |
|---|---|---|---|---|---|
| B4-1 | 종합분석 조합 매칭 | 성향 조합 선택 → 분석 결과 생성 | sequence/best_worst/pair 매칭 정상 (`createOverallAnalysisMatcher`) | □ | □ | □ |
| B4-2 | 응답 필드 차이 | 각 검사의 분석 응답(description 포함 여부) | 검사별 config대로 필드 보존 | □ | □ | □ |
| B4-3 | 기본 종합분석 결과 저장 (`updateDefaultOverallResult`) | 기본값 저장 → 재조회 | 유지 | □ | □ | □ |
| B4-4 | 종합분석 표시 타입 변경 (`updateOverallDisplayType`) | 표시 타입 변경 | 반영 | □ | □ | □ |
| B4-5 | 선택 성향 저장 (`updateSelectedOverallTraits`) | 선택 성향 변경 → 저장 | 반영 | □ | □ | □ |
| B4-6 | ⭐ 결과 전송 (프론트 `useDevSpectrumResultTransfer`) | 결과를 다른 사용자/이메일로 전송 | 전송 성공 toast + 수신측 확인 | □ | □ | □ |
| B4-7 | 결과 이미지 변환 (`convertImageUrlToBase64`) | 외부 이미지 포함 결과 전송 | 이미지 정상 첨부 | □ | □ | □ |

### B-5. 결과 조회 경로 (엔드투엔드)

| # | 테스트 항목 | 기대 결과 | TRI | MC | BAR |
|---|---|---|---|---|---|
| B5-1 | ⭐ 검사 구성 → 응시 → 결과 조회 | 관리자에서 검사 구성 저장 → 실제 응시 화면 진입 → 결과 리포트 | 전체 경로 무결 | □ | □ | □ |
| B5-2 | 결과 리포트 화면 | `DevSpectrumReportView` 정상 렌더 (차트/성향 분포) | □ | □ | □ |
| B5-3 | 결과 이력 화면 | `DevSpectrumResultHistoryBoard` 목록/상세 | □ | □ | □ |
| B5-4 | 시트 비디오 렌더링 | 시트 대표 이미지 자리에 mp4 설정 | **비디오 정상 렌더 + 컨트롤러 숨김** | □ | □ | □ |
| B5-5 | 다음 질문 버튼 강조 | 답변 완료 시 다음 질문 버튼 | **파란 외곽선 깜박임 (기존 → 제거 확인)** | □ | □ | □ |

---

## C. 공통 회귀 확인 (분할 영향 범위)

| # | 테스트 항목 | 기대 결과 | 체크 |
|---|---|---|---|
| C-1 | 콘솔 에러 | 각 화면 진입/조작 중 console에 빨간 에러·React 경고 없음 | □ |
| C-2 | 새로고침 유지 | 각 저장 후 새로고침 → 설정 유지 | □ |
| C-3 | 다이얼로그 오픈/클로즈 | 모든 다이얼로그 열기·닫기·취소가 자연스럽게 동작 | □ |
| C-4 | 로딩 상태 | 로딩 스피너가 사라지고 콘텐츠 표시 | □ |
| C-5 | 뷰포트 6구간 | 분할 컴포넌트 화면을 모바일~UFHD(767/768/1279/1280/1919/1920/2559/2560/3839/3840px)에서 확인 | 요소 겹침·가로 스크롤 없음 | □ |
| C-6 | 권한 없음 | 일반 계정으로 관리자 페이지 접근 시도 | 차단/리다이렉트 | □ |

---

## 결과 기록 양식

```
검증 일시: ____-__-__ __:__
검증자: ______
브라우저/해상도: ______
백엔드 커밋: ______ / 프론트 커밋: ______

통과: __/__  |  실패: __건

실패 상세 (항목번호 / 재현 절차 / 실제 결과 / 기대 결과):
1.
2.
```
